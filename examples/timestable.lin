[ 1 13 .-> ].+ \* table ( \ 3padl ).' 12cols ( <ws outln ).'



# EXPLANATION

Outputs the multiplication table from 1 to 12.

- `[ 1 13 .-> ].+ \* table` creates the table using a Cartesian product.
- `( \ 3padl ).'` formats each number to length 3 using spaces.
- `12cols ( <ws outln ).'` reshapes the table and prints each row.